name: abyss-walker
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.0.0
  - numpy>=1.24.0
  - pip
  - pip:
    - gymnasium>=0.29.0
    - tensorboard>=2.14.0
    - pytest>=7.4.0
